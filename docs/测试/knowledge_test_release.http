

POST http://124.220.178.83:8082/aisearch/knowledge/uploadAndBuild
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file"; filename="劳动人事争议仲裁办案规则.docx"
Content-Type: application/octet-stream

< K:\software_data\微信数据\WeChat Files\shoujife778989\FileStorage\File\2025-08\test_speed\test_speed\劳动人事争议仲裁办案规则.docx
------WebKitFormBoundary7MA4YWxkTrZu0gW--
### 1. 文件上传并构建知识库
### 调用成功结果：文件上传并构建成功
### 调用失败结果：文件上传并构建失败

POST http://124.220.178.83:8082/aisearch/knowledge/buildStatus
Content-Type: application/json

### 2. 状态获取
### 调用成功结果：success
### 调用失败结果：fail message:构建中



POST http://124.220.178.83:8082/aisearch/knowledge/kgsearch
Content-Type: application/json

{
  "query": "仲裁委员会在聘任仲裁员时，应从哪些人员中选聘？|仲裁委员会,聘任仲裁员,选聘人员"
}

### 3. 知识库检索
### 调用成功结果：文档:\n<文档>....</文档>
### 调用失败结果：没有找到相关信息
### ！！！注意这个接口的query参数，第一个是用户的原始输入，第二个是实体关键词数组，用|隔开，这意味着，智能体平台在基于这个接口做工具之前，要先调用大模型将用户的原始输入转换成这个格式（第一个是用户的原始输入，第二个是实体关键词数组，用|隔开），参考提示词如下：

### 提示词：
从用户输入中提取出实体关键词，生成一个关键词数组，并将用户原始输入与该数组通过“|”符号拼接，形成一个查询字符串。格式如下：

用户输入|用逗号隔开的实体关键词数组。

举例如下：
用户输入：介绍052D型驱逐舰的雷达系统。
关键词数组：052D型驱逐舰,雷达系统
生成的查询字符串：介绍052D型驱逐舰的雷达系统。|052D型驱逐舰,雷达系统


### 4. 知识库重置
POST http://124.220.178.83:8082/aisearch/knowledge/reset
Content-Type: application/json

### 4. 知识库重置
### 调用成功结果：success
### 该接口会将服务端之前上传的文件删除，同时将数据库删除并重建。
